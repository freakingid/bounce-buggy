import { test } from 'node:test';

// Module under test: src/world/courses.js (getCourse) — stub until Phase 7.

test.todo('COURSE_COUNT courses are defined (spec §6.2)');
test.todo('every course is completable — no unjumpable gaps (CLAUDE.md §6)');
test.todo('every course is completable — no impossible geometry (CLAUDE.md §6)');
test.todo('a rock funnel always precedes a required road jump (spec §5.4)');
test.todo('level 1 has no season; levels 2+ follow Spring/Summer/Fall/Winter (spec §6.3)');
