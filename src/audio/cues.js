/**
 * Cue -> sound mapping (spec §8): jump warning beep, engine, jump, player
 * crash, enemy destroyed, bonus/tally, gameplay music, attract music.
 * @module audio/cues
 */

/**
 * Play a named audio cue.
 * @param {string} name
 * @returns {void}
 */
export function playCue(name) {
  throw new Error('not implemented — see Phase 10');
}
