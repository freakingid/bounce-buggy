/**
 * Debris entity, dropped by trucks. Static once dropped; scrolls with the
 * road (spec §5.3). Follows the uniform entity contract (CLAUDE.md §5).
 * @module entities/debris
 */

/**
 * Create a debris entity.
 * @param {{ x: number, y: number }} config
 * @returns {object} Debris entity instance.
 */
export function createDebris(config) {
  throw new Error('not implemented — see Phase 6');
}
