/**
 * Enemy car entity (tractor, cycle, skull/hearse, race car, white/yellow/
 * green/blue/brown car). Follows the uniform entity contract (CLAUDE.md §5).
 * @module entities/enemyCar
 */

/**
 * Create an enemy car entity.
 * @param {{ type: string, x: number, y: number }} config
 * @returns {object} Enemy car entity instance.
 */
export function createEnemyCar(config) {
  throw new Error('not implemented — see Phase 5');
}
