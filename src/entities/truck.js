/**
 * Dump truck entity. Not bumpable (spec §5.1); a separate entity class from
 * enemy cars. Follows the uniform entity contract (CLAUDE.md §5).
 * @module entities/truck
 */

/**
 * Create a truck entity.
 * @param {{ x: number, y: number }} config
 * @returns {object} Truck entity instance.
 */
export function createTruck(config) {
  throw new Error('not implemented — see Phase 6');
}
