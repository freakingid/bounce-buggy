/**
 * The 32 course tables (spec §6.2): courses 1-3 unique, followed by a
 * recycled structural cycle with per-level hazard escalation.
 * @module world/courses
 */

/**
 * Get the course definition for a level.
 * @param {number} level - 1-based level number.
 * @returns {object} Course definition.
 */
export function getCourse(level) {
  throw new Error('not implemented — see Phase 7');
}
