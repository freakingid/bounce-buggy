/**
 * Entry point. Bootstraps the game engine.
 *
 * Nothing is wired up yet — the render loop, canvas setup, and game state
 * machine are built in later phases. This file intentionally does nothing
 * at load time so `index.html` opens without console errors.
 * @module main
 */
