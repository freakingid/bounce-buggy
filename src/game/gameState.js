/**
 * Top-level game state FSM: attract / play / tally / gameover.
 * @module game/gameState
 */

/**
 * Create the game state machine.
 * @returns {object} Game state machine instance.
 */
export function createGameState() {
  throw new Error('not implemented — see Phase 9');
}
