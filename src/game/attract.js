/**
 * Attract mode: title/demo sequence shown outside active play.
 * @module game/attract
 */

/**
 * Create attract mode.
 * @returns {{ update: (dt: number) => void, draw: (ctx: CanvasRenderingContext2D) => void }}
 */
export function createAttractMode() {
  throw new Error('not implemented — see Phase 9');
}
