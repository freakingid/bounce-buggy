/**
 * HUD: speedometer, JUMP OK indicator, score, cars remaining, G indicator,
 * the flashing "!" jump warning (spec §9). Exact pixel coordinates are
 * `[?]` — keep all positions in one place for later correction.
 * @module game/hud
 */

/**
 * Draw the HUD.
 * @param {CanvasRenderingContext2D} ctx
 * @param {object} state - Current game/play state.
 * @returns {void}
 */
export function drawHud(ctx, state) {
  throw new Error('not implemented — see Phase 9');
}
